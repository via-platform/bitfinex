{
  "name": "bitfinex",
  "version": "0.0.0",
  "description": "Connect to the Bitfinex exchange.",
  "main": "./lib/main",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "via",
    "trading",
    "title",
    "bar"
  ],
  "exchange": "bitfinex",
  "repository": "https://github.com/via-platform/bitfinex",
  "author": "Dakota Gordon",
  "engines": {
    "via": "*"
  },
  "dependencies": {
    "axios": "^0.16.2",
    "underscore-plus": "^1.6.6",
    "uuid": "^3.1.0",
    "ws": "^3.3.0"
  },
  "configSchema": {},
  "accountConfigSchema": {
    "accountName": {
      "type": "string",
      "default": "Bitfinex Trading Account",
      "description": "Give this account a short name for your own personal reference."
    },
    "apiKey": {
      "title": "API Key",
      "type": "string",
      "default": "",
      "description": "The API key generated by Bitfinex."
    },
    "apiSecret": {
      "title": "Secret Key",
      "type": "string",
      "default": "",
      "description": "The API secret key generated by Bitfinex."
    }
  }
}
